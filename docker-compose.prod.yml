services:
  api:
    container_name: myapp-api-prod
    environment:
      - API_URL=https://api.example.com
      - API_KEY=${PROD_API_KEY}
      - LOG_LEVEL=info
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
